name: Run prevody ZS patch

on:
  push:
    paths:
      - '.github/workflows/run-prevody-zs-result-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/patch_prevody_zs_limits.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prevody.html
          git diff --cached --quiet || git commit -m "Limit and group ZŠ conversion results"
          git push
